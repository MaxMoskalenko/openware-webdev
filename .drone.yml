kind: pipeline
type: docker
name: default

steps:
- name: "Verify & build"
  image: node:alpine
  commands:
    - cd supabase_task
    - npm run build
    - npm start
     
trigger:
  branch:
    - dev
  event:
    - push